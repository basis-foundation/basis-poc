# adapters — protocol bridge layer
# Each adapter translates between a physical protocol and the BASIS internal event model.
# Stage 6: MQTT adapter only. BACnet/Modbus adapters are planned for Stage 10.
